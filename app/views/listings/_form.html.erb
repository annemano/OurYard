<%= simple_form_for(listing) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :price_per_hour %>
  <%= f.input :photo, as: :file %>
  <%= f.submit 'Create new listing', class: "btn btn-success"%>
<% end %>
