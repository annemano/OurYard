<%= simple_form_for(listing) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :price_per_hour, placeholder: '$'%>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit 'Create new listing', class: "btn"%>
<% end %>
